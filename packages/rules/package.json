{
  "name": "oculus-rules",
  "version": "0.1.0",
  "type": "module",
  "description": "Rule engines for oculus — native pattern rules, tree-sitter AST rules, and semantic analysis (type checkers + semgrep)",
  "main": "src/index.ts",
  "types": "src/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "oculus-native": "file:../native",
    "tree-sitter": "0.22.4",
    "tree-sitter-typescript": "*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  },
  "files": [
    "src/",
    "rules/"
  ]
}
